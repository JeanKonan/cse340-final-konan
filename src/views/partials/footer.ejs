        </main>
        <footer>

        </footer>

        <a href="/cart" class="floating-cart" aria-label="Open cart">
            <span class="material-symbols-outlined">
                shopping_cart
            </span>
            <% if (cartItemCount > 0) { %>
                <span class="floating-cart-badge"><%= cartItemCount %></span>
            <% } %>
        </a>

    </body>

</html>