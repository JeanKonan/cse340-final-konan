<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=shopping_cart" />
    
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/header.css">
    <link rel="stylesheet" href="/styles/footer.css">
    <%- renderStyles() %>
    <%- renderScripts() %>
    <title>Yofe Eats</title>
</head>
<body>
    <header>
        <p>
            Eat healthy, Live well <br>
            Your health, Our priority
        </p>
        <h4 id="logo"><a href="/">Yofe Eats</a></h4>

        <nav>
            <a href="/offers" <% if (title === 'offers') { %>class="active"<% } %>>Offers</a>
            <a href="/contact" <% if (title === 'contact') { %>class="active"<% } %>>Account</a>
            <a href="/menu" <% if (title === 'Menu') { %>class="active"<% } %>>Menu</a>
        </nav>
    </header>